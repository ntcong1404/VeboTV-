const routes = {
  home: "/",
  profile: "/profile",
  result: "/result",
  schedule: "/schedule",
  news: "/news",
  register: "/register",
  match: "/match-live/:match/:id",
  league: "/league/:slug/:id",
};

export default routes;
