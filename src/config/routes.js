const routes = {
  home: "/",
  profile: "/profile",
  result: "/result/:date",
  schedule: "/schedule",
  news: "/news/page/:number",
  register: "/register",
  match: "/match-live/:match/:id",
  league: "/league/:slug/:id",
};

export default routes;
