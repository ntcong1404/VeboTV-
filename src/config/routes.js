const routes = {
  home: "/",
  result: "/result",
  schedule: "/schedule",
  news: "/news",
  highlight: "/highlight",
  scrutiny: "/scrutiny",
  replay: "/replay",
  profile: "/profile",
  register: "/register",
  match: "/match-live/:match/:id",
  league: "/league/:slug/:id",
  recordMatch: "/:status/:slug/:id",
};

export default routes;
